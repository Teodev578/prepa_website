# Team / enterprise overrides for _bmad/config.toml.
# Committed to the repo — applies to every developer on the project.
# Tables deep-merge over base config; keyed entries merge by key.
#
# ── Overrides appliqués par audit 2026-09-14 ──────────────────────
# P1: Alignement de la langue sur le corpus réel (tout le projet est en français)
# P2: Correction du chemin de sortie BMAD (était résolu à la racine du projet)

[core]
document_output_language = "French"
output_folder = "{project-root}/.agents/_bmad-output"
